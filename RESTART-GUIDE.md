# Steps to Restart Dev Server

1. If the dev server is running, press Ctrl+C to stop it
2. Run: npm run dev
3. Wait for "Ready" message
4. Try the application again

# The server must be restarted because Next.js only loads .env files at startup
